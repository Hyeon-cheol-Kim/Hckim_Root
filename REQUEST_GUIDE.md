# 작업 요청 가이드 (Request Guide)

> 이 문서는 **Claude에게 작업을 요청할 때 어떤 정보를 주면 가장 빠르고 정확하게
> 결과를 받을 수 있는지** 정리한 템플릿입니다.
> 다음 요청부터 이 문서의 항목을 채워서 전달하면 됩니다.
> 모든 항목을 다 채울 필요는 없고, **굵게 표시된 항목**만 있어도 시작할 수 있습니다.

---

## 0. 한 줄 요약 (TL;DR)
- **무엇을 / 누구를 위해 / 왜** 만드는지 한 문장.
  - 예) "Android 커널 ftrace 로그를 골라서 저장하는 CLI 도구를 만들고 싶다."

---

## 1. 목적 & 배경
- **이 작업으로 해결하려는 문제**는 무엇인가?
- 지금은 어떻게 하고 있고, 무엇이 불편한가? (있으면)
- 최종 사용자(나/팀/고객)는 누구인가?

## 2. 작업 유형
아래 중 해당하는 것을 표시하면 접근 방식을 빠르게 정합니다.
- [ ] 신규 기능/도구 제작
- [ ] 기존 코드 수정/리팩터링
- [ ] 버그 수정
- [ ] 질문/조사(코드 분석, 방법 문의)
- [ ] 문서 작성
- [ ] 기타: ______

## 3. 기능 요구사항 (가장 중요)
- **핵심 기능 목록** (동사로, 우선순위 순)
- **동작 시퀀스**: 사용자가 1) → 2) → 3) 순서로 무엇을 하고 무엇이 보이는가?
  - 예)
    1. adb 디바이스 목록 출력 → 선택해 연결
    2. ftrace 옵션 + '설정완료' 메뉴 출력 → 완료 전까지 선택 옵션 enable
    3. 버퍼 최대화 + 저장 파일명 입력
    4. 캡처 시작 → 저장
- **하지 말아야 할 것 / 범위 밖**(있으면 명시): 과한 작업 방지에 큰 도움

## 4. 입력 / 출력 명세
- **입력**: 형식, 예시 값, 어디서 오는지(파일/사용자입력/장치 등)
- **출력**: 형식(.log/.csv/.png/콘솔 등), 파일명 규칙, 저장 위치
- 출력 예시(샘플 한 조각)가 있으면 정확도가 크게 올라갑니다.

## 5. 대상 환경 & 제약
- **실행 환경**: OS(Windows/Linux/macOS), Python 버전 등
- 대상 기기/플랫폼: (예: Android, root 필요 여부)
- **의존성 정책**: 표준 라이브러리만 / 특정 패키지 허용·금지
- 오프라인/네트워크 제약, 권한 제약 등

## 6. 기술 선호
- 언어 / 프레임워크 / 라이브러리 선호 (예: Python, PyQt, Tkinter)
- 코드 스타일: 주석 언어(한글/영어), 주석 밀도, 네이밍 컨벤션
  - (이 저장소 기본: **한글 주석**, 풍부한 설명, 박스형 섹션 구분)

## 7. 비기능 요구사항 (품질 기대치)
- **예외/입력 검증** 수준: "전수 방어" vs "핵심만" (기본은 방어적으로 처리)
- 성능/용량 고려(대용량 로그, 실시간성 등)
- 보안/개인정보 고려
- 테스트 필요 여부(단위 테스트/스모크 테스트)

## 8. 확장성 / 향후 계획
- 나중에 추가할 가능성이 있는 기능 (미리 알면 구조를 그에 맞게 설계)
  - 예) "추후 Windows GUI로 확장" → 로직/UI 분리 설계

## 9. 산출물 형태
- 단일 스크립트 / 패키지(폴더 구조) / 라이브러리 중 무엇?
- README, 사용법 문서 필요 여부
- 파일명/위치 지정(있으면)

## 10. 검증 방법 (완료 기준)
- "무엇이 되면 완료인가?" (수용 기준, Acceptance Criteria)
- 내가 직접 돌려볼 수 있는 환경이 있는지 / 실기기 필요 여부
- 샘플 데이터 제공 가능 여부

## 11. Git / 협업 규칙
- **작업 브랜치**: (예: `claude/xxx`)
- 커밋/푸시 시점: 자동 vs 확인 후
- PR 생성 여부 (기본: **명시 요청 시에만** 생성)

## 12. 참고 자료
- 관련 문서/링크/이슈
- 비슷한 예시(기존 코드, 다른 도구)
- 제약 명세서/사양서

---

## ✅ 최소 체크리스트 (이것만 있어도 시작 가능)
1. **무엇을** 만드는가 (목적)
2. **동작 순서** (사용자 흐름)
3. **입력/출력** 형식
4. **실행 환경**(OS/언어)
5. **완료 기준** (어떻게 확인)

---

## 📋 복사용 빠른 템플릿
```
[목적] 
[작업유형] 신규/수정/버그/질문/문서
[기능 요구사항(우선순위 순)]
 1.
 2.
[동작 시퀀스]
 1) 
 2) 
[입력] 
[출력] (형식/파일명/위치)
[실행 환경] OS / 언어·버전
[의존성 정책] 표준만 / 허용 패키지:
[코드 스타일] 주석 언어 등
[예외처리 기대] 전수 / 핵심만
[향후 확장] 
[산출물] 스크립트/패키지/README
[완료 기준] 
[브랜치] 
[참고] 
```

---

## 부록 A. 좋은 요청 예시 (이번 ftrace 도구)
> 아래는 실제로 잘 동작했던 요청을 위 템플릿에 맞춰 정리한 예시입니다.

- **목적**: Android system kernel이 지원하는 trace 로그를 선택·출력·저장하는 도구
- **작업유형**: 신규 도구 제작
- **동작 시퀀스**:
  1. PC-폰 adb USB 연결 후 실행 → 연결된 adb 목록 출력 → 입력하면 연결
  2. 설정 가능한 ftrace 옵션 + '설정완료' 메뉴 출력 → 완료 전까지 선택 옵션 enable
  3. 버퍼 사이즈 최대화 등 수행 + 저장 파일명 입력
  4. 출력·저장 시작 시 저장 시작
- **출력**: `.log` 파일 형식으로 저장
- **향후 확장**: Windows GUI 도구로 확장 가능성 → UI 출력 고려해 설계 필요
- **완료 기준**: 위 시퀀스대로 동작, 로그 파일 저장

### 이 예시가 좋았던 이유
- 동작 시퀀스가 **번호 매겨 명확**했다 → 구현 흐름이 바로 잡힘
- **출력 형식**(.log)이 분명했다
- **향후 확장**(GUI)을 미리 알려줘서 처음부터 로직/UI를 분리 설계할 수 있었다

### 더 있었으면 더 좋았을 정보 (참고)
- 실행 환경 OS(개발/사용 PC가 Windows인지 Linux인지)
- 어떤 ftrace 이벤트를 주로 볼지(예: sched, irq) — 기본 추천 세트 구성에 도움
- root 권한 가능 여부(adb root vs su)
- 의존성 정책(표준 라이브러리만 쓸지)

---

## 부록 B. 내가(Claude) 기본으로 가정하는 것들
명시하지 않으면 아래 기본값으로 진행하고, 결과에 그 사실을 알려드립니다.
- 코드 주석: **한글**, 설명 충실
- 예외/입력 검증: **방어적으로** 처리
- 의존성: 가능하면 **표준 라이브러리 우선**
- 산출물: 재사용·확장 쉬운 **모듈 구조**
- Git: 지정된 **작업 브랜치에 커밋·푸시**, PR은 **요청 시에만**
- 불명확하거나 되돌리기 어려운 결정은 **먼저 질문**
