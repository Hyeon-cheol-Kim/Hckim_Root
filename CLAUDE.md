# 프로젝트 작업 규칙 (CLAUDE.md)

이 저장소에서 코드를 작성/수정하는 모든 작업은 아래 규칙을 따른다.

## 주석 (필수)

- **모든 코드에 상세 주석을 단다. 주석은 항상 필수다.**
- "무엇을" 보다 **"왜"** 를 설명한다(자명한 코드 반복 설명은 지양).
- 함수/메서드에는 역할·인자·반환·예외를 docstring 으로 남긴다.
- 로직이 빽빽한 부분(파싱 정규식, 키 매칭, 지연 계산, 설치/정리 순서 등)은
  **라인 단위 의도 주석**을 단다.
- 비자명한 상수/매직넘버(예: `sector = LBA × 8`)는 근거를 주석으로 밝힌다.
- 주석 언어는 한국어를 기본으로 한다(이 저장소의 기존 코드와 일관).

## 용어

- 한글 표현은 직관적인 단어를 쓴다. 계층 간 이벤트 연결은 **"흐름 추적"** 으로
  부른다("상관"은 쓰지 않는다).
- 영문 식별자(함수/변수명, 예: `correlation`, `apply_correlation_preset`)는
  한글 용어 변경과 무관하게 그대로 둔다.

## 파일 배치 (폴더 규칙)

- **새 파일은 루트에 흩뿌리지 않는다.** 기능/도구 단위로 **전용 폴더**를 만들어
  그 안에 넣는다(예: `android_ftrace_tool/`, `eye_diagram_converter/`).
- 한 도구에 속하는 **코드·스펙·문서·다이어그램·빌드 스크립트**는 같은 폴더(또는
  그 하위 폴더, 예: `tools/`)에 함께 둔다.
- 폴더명은 도구/기능 이름으로 소문자+`_` 를 쓴다(예: `eye_diagram_converter`).
- **예외(루트 허용)**: 저장소 전역 문서만 루트에 둔다 — `CLAUDE.md`,
  `REQUEST_GUIDE.md`. 저장소 전역 문서/기록은 `docs/`(예: `docs/DEVLOG.md`)에
  모은다. 그 외 코드/자료는 반드시 기능 폴더 안에 둔다.
- 파일을 옮길 때는 이력 보존을 위해 `git mv` 를 쓰고, 옮긴 경로를 참조하는
  문서/코드가 있으면 함께 고친다.

### 현재 폴더 구성

| 폴더 | 내용 |
|------|------|
| `android_ftrace_tool/` | Android ftrace I/O 캡처·흐름 추적 분석 도구(+ `MANUAL.md`) |
| `tools/bpftrace-android/` | aarch64 정적 bpftrace 빌드(위 도구의 하위 도구) |
| `eye_diagram_converter/` | Eye Diagram CSV→이미지 변환기(코드·스펙·블록다이어그램) |

## 구조 (UI / 로직 분리)

- `core.py` 는 **순수 로직**이다: `print`/`input` 등 UI 코드를 넣지 않는다.
  GUI 등에서 재사용할 수 있어야 한다.
- 사용자 입출력은 `cli.py`(또는 별도 UI 계층)에만 둔다.
- 디바이스 미지원 기능은 죽지 않고 **best-effort** 로 처리하고 경고만 남긴다.

## 검증

- 변경 후 `python -m py_compile android_ftrace_tool/*.py` 로 컴파일을 확인한다.
- 디바이스 없이 가능한 부분(파서/분석기/명령 구성)은 가짜 adb 나 샘플 로그로
  동작을 확인한다.

## Git

- 지정된 작업 브랜치에서 개발하고, 명확한 커밋 메시지로 커밋·푸시한다.
- PR 은 사용자가 명시적으로 요청할 때만 만든다.

## 개발 로그 (필수, 누적)

- **대화·결정·구현 내역은 `docs/DEVLOG.md` 에 계속 기록·갱신한다.**
- 새 요청/결정/구현이 있을 때마다 해당 절(기능 기록·커밋 타임라인·미해결)을
  업데이트한다. 기본 단위는 **한 작업 = 한 커밋 + 로그 갱신**.
- 무엇을 했는지보다 **왜 그렇게 결정했는지(맥락·대안)** 를 남긴다.
